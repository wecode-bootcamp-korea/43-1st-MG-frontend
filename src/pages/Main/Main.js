import React from 'react';

const Main = props => {
  //searchKeyword : 검색키워드
  //setBasket : 상품마다 장바구니 버튼을 누르면 장바구니 리스트에 추가해주기 위함
  //selectedCategory : 선택된 카테고리에 따라 그에 맞는 상품을 리스팅하기 위함
  const { searchKeyword, setBasket, selectedCategory } = props;

  return <div>Main</div>;
};

export default Main;
